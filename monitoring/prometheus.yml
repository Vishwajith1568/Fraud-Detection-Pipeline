global:
  scrape_interval: 5s

scrape_configs:
  - job_name: 'node_backend'
    static_configs:
      # Use Docker service name since prometheus is on the same Docker network
      - targets: ['web-backend:4000']